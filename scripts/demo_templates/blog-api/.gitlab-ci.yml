stages:
  - test
  - lint

test:
  stage: test
  image: python:3.12
  script:
    - pip install -e ".[dev]"
    - pytest

lint:
  stage: lint
  image: python:3.12
  script:
    - pip install ruff
    - ruff check src/
